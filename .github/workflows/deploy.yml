name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy-backend:
    name: Deploy Backend (Render auto-deploys on push)
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          echo "Render auto-deploys from GitHub. Ensure render.yaml is connected."

  deploy-frontend:
    name: Deploy Frontend (Vercel auto-deploys on push)
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy
        run: |
          echo "Vercel auto-deploys from GitHub. Ensure project is linked."
